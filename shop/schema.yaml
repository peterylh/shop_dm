version: 2

models:
  # ── Snapshot 维度表 ──────────────────────────────
  - name: dwd_customer
    description: 客户每日快照
    config:
      materialized: snapshot

  - name: dwd_store
    description: 门店每日快照
    config:
      materialized: snapshot

  - name: dwd_product
    description: 商品每日快照
    config:
      materialized: snapshot

  - name: dwd_promotion
    description: 促销活动每日快照
    config:
      materialized: snapshot

  - name: dwd_inventory
    description: 库存每日快照
    config:
      materialized: snapshot

  # ── Incremental 日频事实/汇总 ─────────────────────
  - name: dwd_order_detail
    description: 订单明细事实表
    config:
      materialized: incremental

  - name: dws_store_sales_daily
    description: 门店日销售汇总
    config:
      materialized: incremental

  - name: dws_product_sales_daily
    description: 商品日销售汇总
    config:
      materialized: incremental

  - name: dws_customer_order_summary
    description: 客户订单汇总
    config:
      materialized: incremental

  - name: dws_inventory_daily
    description: 库存日汇总
    config:
      materialized: incremental

  - name: dws_promotion_effect_daily
    description: 促销效果日汇总
    config:
      materialized: incremental

  - name: ads_promotion_roi
    description: 促销 ROI 分析
    config:
      materialized: incremental

  - name: ads_customer_rfm
    description: 客户 RFM 分析
    config:
      materialized: incremental

  - name: ads_product_topn_daily
    description: 商品日销售排行
    config:
      materialized: incremental

  - name: ads_inventory_alert
    description: 库存预警分析
    config:
      materialized: incremental

  # ── Incremental 月频汇总 ─────────────────────────
  - name: dws_category_sales_monthly
    description: 品类月度销售汇总
    config:
      materialized: incremental

  - name: ads_store_performance
    description: 门店绩效评估
    config:
      materialized: incremental

  # ── Full 全量 ───────────────────────────────────
  - name: ads_sales_dashboard
    description: 销售驾驶舱全量汇总
    config:
      materialized: full
